"use client";
import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { donations, donorForDonation } from "@/lib/donations";
import { donors } from "@/lib/donors";
export function DonationsTable() { const [year, setYear] = useState("2026"); const rows = useMemo(() => donations.filter((donation) => donation.year === year), [year]); return <><div className="mb-5 flex items-center justify-between gap-4"><p className="text-sm text-muted-foreground">{rows.length} donazioni registrate</p><div className="w-36"><Select value={year} onValueChange={setYear}><SelectTrigger aria-label="Filtra per anno"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="2026">2026</SelectItem><SelectItem value="2025">2025</SelectItem><SelectItem value="2024">2024</SelectItem></SelectContent></Select></div></div><Table><TableHeader><TableRow><TableHead>Data</TableHead><TableHead>Donatore</TableHead><TableHead className="hidden sm:table-cell">Gruppo sanguigno</TableHead><TableHead className="text-right">Tipologia</TableHead></TableRow></TableHeader><TableBody>{rows.map((donation) => { const donor = donorForDonation(donation, donors); return <TableRow key={donation.id}><TableCell>{donation.date}</TableCell><TableCell><div className="font-medium">{donor?.name ?? "Donatore non trovato"}</div><div className="text-xs text-muted-foreground sm:hidden">Gruppo {donor?.bloodType}</div></TableCell><TableCell className="hidden sm:table-cell"><Badge variant="outline">{donor?.bloodType}</Badge></TableCell><TableCell className="text-right">{donation.type}</TableCell></TableRow>; })}</TableBody></Table></>; }
