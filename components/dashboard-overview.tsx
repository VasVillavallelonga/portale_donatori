"use client";
import { useState } from "react";
import { ArrowUpRight, Droplets, HeartPulse, Users } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { donors } from "@/lib/donors";
import { dashboardByYear, dashboardYears, type DashboardYear } from "@/lib/dashboard";
export function DashboardOverview() { const currentYear = "2026"; const [year, setYear] = useState<DashboardYear>(currentYear); const data = dashboardByYear[year]; const metrics = [{ label: "Donazioni di sangue", value: data.donations, icon: Droplets }, { label: "Donatori attivi", value: data.activeDonors, icon: Users }, { label: "Nuovi donatori", value: data.newDonors, icon: HeartPulse }]; return <><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Panoramica</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Benvenuto, Admin</h1><p className="mt-2 text-muted-foreground">Riepilogo delle donazioni per l’anno selezionato.</p></div><div className="w-full sm:w-40"><label htmlFor="dashboard-year" className="mb-2 block text-sm font-medium">Anno</label><Select value={year} onValueChange={(value) => setYear(value as DashboardYear)}><SelectTrigger id="dashboard-year"><SelectValue /></SelectTrigger><SelectContent>{dashboardYears.map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-4 md:grid-cols-3">{metrics.map(({ label, value, icon: Icon }) => <Card key={label}><CardHeader className="flex-row items-center justify-between space-y-0 pb-2"><CardDescription>{label}</CardDescription><Icon className="h-4 w-4 text-muted-foreground" /></CardHeader><CardContent><div className="text-2xl font-bold">{value}</div><p className="mt-1 flex items-center text-xs text-emerald-700"><ArrowUpRight className="mr-1 h-3 w-3" />{data.change} <span className="ml-1 text-muted-foreground">rispetto all’anno precedente</span></p></CardContent></Card>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-5"><Card className="lg:col-span-3"><CardHeader><CardTitle>Andamento donazioni</CardTitle><CardDescription>Unità raccolte nei primi sei mesi del {year}</CardDescription></CardHeader><CardContent><div className="flex h-52 items-end justify-between gap-3 border-b pb-2">{data.monthlyDonations.map((value, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-sm bg-primary/85" style={{ height: `${Math.round((value / 125) * 100)}%` }} /><span className="text-xs text-muted-foreground">{["Gen", "Feb", "Mar", "Apr", "Mag", "Giu"][index]}</span></div>)}</div><p className="mt-4 text-sm text-muted-foreground">Il confronto tra anni verrà aggiunto qui in una prossima iterazione.</p></CardContent></Card><Card className="lg:col-span-2"><CardHeader><CardTitle>Ultime donazioni</CardTitle><CardDescription>Donatori registrati più di recente</CardDescription></CardHeader><CardContent className="space-y-4">{donors.slice(0, 4).map((donor) => <div key={donor.id} className="flex items-center justify-between"><div><p className="text-sm font-medium">{donor.name}</p><p className="text-xs text-muted-foreground">{donor.lastDonation}</p></div><div className="text-right"><p className="text-sm font-semibold">Gruppo {donor.bloodType}</p><Badge variant="success" className="mt-1">Registrata</Badge></div></div>)}</CardContent></Card></div></>; }
